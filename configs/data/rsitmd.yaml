_target_: src.data.rsitmd_datamodule.RSITMDDataModule #TODO
data_dir: "/home/antonio/projects/mamba-gaia-ir/data/RSITMD"
batch_size: 256 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 4
pin_memory: True
max_length: 24
tokenizer:
  _target_: transformers.AutoTokenizer.from_pretrained
  pretrained_model_name_or_path: gpt2

test_ratio: 0.20