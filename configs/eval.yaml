# @package _global_
# Run: python src/eval.py ckpt_path=path/to/checkpoint.ckpt

defaults:
  - _self_
  - data: gaia # choose datamodule with `test_dataloader()` for evaluation
  - model: mamba3
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: null
