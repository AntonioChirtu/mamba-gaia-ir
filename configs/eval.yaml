# @package _global_

defaults:
  - _self_
  - data: mnist # choose datamodule with `test_dataloader()` for evaluation
  - model: mnist
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: "/home/antonio/projects/mamba-gaia-ir/logs/train/runs/2026-04-28_17-16-04/checkpoints/epoch_079.ckpt"
